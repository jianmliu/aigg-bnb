node_modules/
contracts/out/
contracts/cache/
contracts/broadcast/
# deployment addresses and keys live in environment files, never in git
deployments/*.json
deployments/*.json.tmp
deployments/*.json.lock
.env
.env.*
relayer/config.json
env.*.txt
frontend/dist/

# the gateway's calls (gateway/gateway.mjs, GATEWAY_STATE): task ids and receipts, written next to where it runs
gateway-state.json*
