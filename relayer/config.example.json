{
  "_comment": "Optional. Addresses, RPC, the relayer key and the MEP ids come from the environment (source .env.<network> written by deploy.sh; add PORW_RELAYER_KEY and PORW_MEP_IDS). Environment always wins over this file. Never put keys or addresses in git.",
  "relayPort": 8787,
  "apiPort": 8788,
  "host": "0.0.0.0",
  "pollMs": 2000,
  "name": "bnb-relayer-1",
  "mepNames": { "0x<mep id>": "flywire-fafb-v783-min5" }
}
